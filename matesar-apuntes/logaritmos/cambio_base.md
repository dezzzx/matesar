# Cambio de base

  log_a(x) = log_b(x) / log_b(a)

  Muy útil para calcular con calculadora (que solo tiene log y ln):

  log_a(x) = ln(x) / ln(a) = log(x) / log(a)

  Ejemplo: log_2(10) = ln(10)/ln(2) ≈ 3.3219
